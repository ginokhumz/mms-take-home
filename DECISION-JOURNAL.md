# Decision journal

Write in the first person. Write as you work, not at the end. Rough timestamps are fine, and
"about 40 minutes later" is a valid timestamp.

This file is marked. We read it for three things: how your understanding changed, what you tried
and abandoned, and how you used any AI assistant. See `AI-POLICY.md`.

Keep it honest. "I did not understand the deletion requirement for the first hour" is a good
entry. It tells us more than a tidy narrative does.

Delete these instructions and the example entry before you submit, or leave them. We do not mind.

---

## Time log

Fill this in at the end. Wall-clock hours, honestly.

| Piece | Hours |
|-------|-------|
| Problem 1, design document | |
| Problem 1b, frontend | |
| Problem 2, critique and extend | |
| Journal and admin | |
| **Total** | |

---

## Entries

### Your entries start here

**Wednesday, about 11:00** Read instructions and browsed the 2 problems. Created the repos and initial commits. 

**Wednesday, about 11:30** Started with Problem 1. Read through the documents to get understanding of what needs to be done and what is required.

**Wednesday, about 12:00** Initialized structure for AI agents that covers the whole project.

**Wednesday, about 21:00** Planned out the plan and capacity section

**Wednesday, about 21:30** Planned out the design and core components

**Wednesday, about 21:45** Planned out api contract

**Wednesday, about 22:30** Wrote the data model. Writing the tables out caught three places where
the earlier sections were wrong, which is the point of doing it after §5 rather than before.

**Wednesday, about 23:00** Wrote the Scaling and bottlenecks section.

**Wednesday, about 23:20** Wrote the Failure modes and reliability section.

**Wednesday, about 23:40** Wrote the security section.

**Thursday, about 00:10** Wrote the Deployment and observability section.

**Thursday, about 00:20** Wrote the Decision records section.

**Thursday, about 00:40** Wrote the Decision records section.

**Thursday, about 21:00** Start on front end build plan docs

**Thursday, about 22:30** Start on FE build
---

## Changes of mind

List the decisions you reversed. One line each: what you first chose, what you moved to, and what
made you move.

| First choice | Moved to | What changed my mind |
|--------------|----------|----------------------|
| Post row ~600 B, 900 GB/month (§4.3) | ~368 B, 560 GB/month raw and 1.68 TB at RF 3 (§6.2) | I wrote 600 B in §4 without a breakdown. Adding the fields up in §6.2 gave 230 B, 368 B with wide-column overhead. I had also never mentioned replication, which matters more than the row size |
| Idempotency key written "in the same partition transaction as the post row" (§5.4) | Reserve the `post_id` in the key row first, then write the post, then mark completed | Laying out the tables in §6.8 made it obvious the two are in different partitions and Cassandra cannot write them atomically. The original claim was just wrong |
| Timeline store had a "cold backing store" behind the 800-entry Redis list (§3.2 diagram) | No cold copy; deep pages come from `posts_by_author` | §4.7 already said entries past 800 are not stored. The diagram label contradicted it and would have meant a second store to keep consistent for no gain |
| One flat `followers_by_user` partition per user | Bucketed at 10,000 edges per partition, in append order | 3M followers in one partition is 48 MB and 3M rows, over both Cassandra guidelines. Append order rather than a hash because fanout and purge both walk buckets and want to checkpoint |

---

## Dead ends

List the approaches you tried and abandoned. We read this section closely. An empty section is a
weaker signal than a full one.

| Approach | Why I abandoned it |
|----------|--------------------|
| | |

---

## AI usage log

One row per use. Add rows as you go. If you used no AI assistant at all, write "none" and say so
in the debrief.

| When | What I asked | What came back | What I kept, changed or discarded, and why |
|------|--------------|----------------|--------------------------------------------|
| Wednesday 12:00 | Instructed Claude to write a context file from the instructions that will be used during developement | A claude file and settings file for permissions for the tools | Nothing yet. It looks solid, I'll update where I see it misbehaving |
| Wednesday (§1 drafting) | Asked Claude to expand on use cases and scope | Draft covering: prioritised use cases, exclusions with reasons, the fanout-vs-skew arithmetic (20,000 top accounts × 1M = 20B edges vs 20M × 50 = 1B), an adopted reading (follower counts include inactive registered accounts), and an assumptions table | Moved the arithmatic to section 2 because that is the best place for it, remove repetition |
| Wednesday (§3/§4 drafting) | Asked Claude to propose the high-level design diagram and the core components | Hybrid fanout (push under 100k followers, read-time merge above), Mermaid diagram, write/read path prose, and 13 component subsections each with store / scaling / unavailability. Grounding arithmetic computed in the shell: 579 posts/s avg, 1,736/s at 3× peak, 28,935 timeline writes/s, one 3M-follower post = 3M writes ≈ 104 s of the whole average fanout budget, 384 GB materialised timelines, ~405 TB/month images | Kept the hybrid and the 104 s figure — that number is the argument for it. Kept the decision that timeline entries store `(post_id, author_id)` only and never the body, because it makes §11.2 (edit propagation) and §11.4 (purge) nearly free. Still to check against myself: the 100,000-follower wide/narrow threshold is asserted, not derived — I need to justify it in §11.1 or move it. Peak multiplier of 3× is used here before §2 states it; §2 must state it or the two sections disagree |
| Wednesday (§5 drafting) | Asked Claude to propose the API contract for §5 | A full contract: 10 endpoints (publish, home timeline, single post, PATCH edit, revisions, search, follow, unfollow, delete account, presigned media), one shared `Post` object, Bearer JWT with a 15-min access token plus rotating refresh cookie and a revoked-`sid` set, opaque base64 descending post-ID cursor, `Idempotency-Key` on publish with a stored `(user_id, key) → response` record, and an error envelope with a machine-readable `retryable` flag. Verified in the shell that the two example cursor strings actually base64-decode to `{"v":1,"b":"..."}` and that the second is lower than the first | Kept the shape. The parts I want to be able to defend out loud and believe: IDs as strings (Snowflake is 64-bit, JS is safe to 2^53−1), `retryable` as an explicit field rather than inferred from the status (409 covers both `edit_window_closed`, which is permanent, and `idempotency_in_progress`, which is not), and reusing the timeline cursor for search since ranking is out of scope so both sort by post ID descending. Changed: made `degraded: true` a 200-with-banner rather than an error, to match §4.7 which already says the timeline serves a degraded page from the pull path. Still unverified and flagged in the entry above: the rate-limit numbers and the per-request tombstone lookup are asserted, not derived from §2 |
| Wednesday (§6 drafting) | Asked Claude to write the data model | CQL for `posts`, `post_revisions`, `posts_by_author`, `followers_by_user`/`following_by_user`, `users`, `media`, `idempotency_keys`; the Redis shapes for the timeline list and caches; the OpenSearch document; an ER diagram; an access-pattern table mapping every §5 endpoint to its keys. Arithmetic computed in the shell: 368 B/post row and 560 GB/month raw (1.68 TB at RF 3), 107M revision rows/month adding 7%, a 3M-follower partition at 48 MB and 3M rows against Cassandra's ~100 MB/~100k-row guidance, 300 buckets at 288 s each inside the 24 h purge budget, 17 GB for the follow-graph read cache, 16 KB for the tombstone set, 384 GB for timelines (matches §4.7) | Kept the bucketing, the reserve-then-write idempotency protocol and the `tlmeta` key — that last one is small but it is the only thing that lets the API tell an empty timeline apart from a lost shard, which §5 already promised via `degraded`. Kept `author_id` in the timeline entry once I worked out the filter runs before hydration, so the merge cannot fetch the author. Changed three earlier sections that this contradicted (logged as changes of mind). Still asserted, not derived: 5% of posts get edited, 1.4 edits each, 140-char average post, 0.01% daily deletion rate — all four are guesses and I have marked them as assumptions in the text |
| Wednesday (§6 revision) | Asked Claude what the `lang` field on the post was, then asked it to remove it | It found `lang` in three places (`posts` table, the row-size sum, the search document), none explained, never in the §5 contract, and nothing producing or reading it. It removed it and redid the sums it fed: post row 230 → 222 B, 368 → 355 B with overhead, 540 GB/month raw (1.62 TB at RF 3), revisions 38 GB/month, search doc ~210 B → 0.73 TB/month per replica; §4.3 updated to match | Removed. It had no source and no consumer, so it was a §5/§6 inconsistency I could not have defended. Language-aware search is not in scope |
| Wednesday (§6.6 revision) | Asked Claude to explain `bucket` in `followers_by_user`, then to write up the gaps it raised | Explanation: buckets split each follower list into 10,000-edge partitions because a 3M-follower account is 3M rows, 30x Cassandra's ~100k-row guidance. Gaps: overfill under concurrent follows, holes left by unfollows, the three follow writes not being atomic, and 16 B/edge leaving out overhead. It added all four to §6.6: edge size recalculated at ~26 B (78 MB unbucketed, 260 KB per bucket, so row count is the real reason to split); `users.active_bucket` plus a `bucket_fill` counter table with an LWT to advance the bucket, replacing the vague "counter row"; overfill ≈ 1,000 follows/s x 50 ms = ~50 edges (both figures assumed); holes accepted, not compacted; edge pair in a multi-partition logged batch, counters allowed to drift. Also fixed §4.10, which still said the partition key was `user_id` alone, and the §6.12 follow row | Kept. The original "a counter row holds active_bucket" could not work, because a counter cannot serve as a checked pointer. The 1,000 follows/s and 50 ms LWT figures are my assumptions, not measurements |
| Wednesday (§7 drafting) | Asked Claude to write §7, scaling and bottlenecks | A ranked list by headroom multiple (capacity ÷ peak demand) rather than by absolute size. Arithmetic in the shell: timeline cluster sized by memory at 384 GB = 8 shards = ~400k entries/s, against 86,806 entries/s peak fanout = 4.6x headroom on the mean — but three plausible follower mixes (0.2% of posts at 60k followers, 0.1% at 60k, 0.5% at 20k) give 295k / 191k / 260k entries/s, i.e. 1.4x–2.1x. One 99,999-follower post = 0.25 s of the whole cluster, so 4 such posts per second saturate it against 1,736 posts/s at peak. Post cache second: 347,222 hydrations/s at peak, and a 95%→90% hit-rate drop doubles post-store reads from 17.4k to 34.7k/s. Search third: a ~2.0 s latency budget inside the 5 s SLO, so it breaks on lag not throughput. Then read-time merge, purge (3M ÷ 86,400 = 35 deletes/s), image egress (300 TB/day, 6 TB/day origin at 98% CDN hit) | Kept. The point I want to be able to make out loud is that the mean-based 4.6x headroom is a fiction — fanout demand is set by the follower distribution, and the prompt only gives the mean, which is exactly the trap §2.1 already flagged. Also kept the framing that the 100k wide/narrow threshold is a runtime dial that converts write amplification into read amplification, and that #1's mitigation pushes load into #4, so the two are coupled. **Read ratio: §7 states 25 timeline requests per active user per day → 500M/day → 12.4:1 overall. §2 is not written yet and must state exactly this or the two sections disagree.** Still assumed, not derived: the 0.1–0.5% near-threshold post fraction, the 95% cache hit rate, 100k ops/s per Redis shard, 15% of posts carrying an image, 200 KB per delivered variant. The cache hit rate is flagged in the text as something to measure, and belongs in §13 |
| Wednesday (§8 drafting) | Asked Claude to write §8, failure modes and reliability | A blast-radius table per dependency, then the six named failures with recovery arithmetic computed in the shell: a 5-min fanout outage = 26.0M entries backlog, draining at 400k − 86.8k = 313k/s surplus in 83 s, but only 248 s at the 1.4x-headroom mix from §7.2; a 10-min indexer stall = 1.04M docs draining at 3,500 − 1,736 = 1,764/s in 590 s (~1:1); image-store failure split into presign / pre-finalise / delivery, with 260 of 1,736 posts/s blocked at the 15% image rate; one lost timeline shard = 2.5M users, and rebuilding them within an hour = 694 users/s × 50 followees = 34,722 reads/s against a post store sized for ~25,000 — i.e. the recovery is bigger than the failure; quorum behaviour for the post store at RF 3 / `LOCAL_QUORUM` across 3 AZs; a purge crash costing at most one 10,000-edge bucket of 300. Then a per-read-path consistency table with staleness windows | Kept. The line I want to be able to reproduce live is 34,722 vs 25,000: it is what forces a replica per timeline shard (an extra 384 GB of RAM) instead of the lazy rebuild §4.7 previously described alone, so I edited §4.7 to match rather than let the two sections disagree. Also kept the two admitted give-ups — no monotonic reads across a paginating session, and up to 48 h staleness on an author's display name, where the alternative is 347k extra reads/s at peak. Changed: added a §1.3 assumption row for the self-edge (an author's own post in their own home timeline), because §8.7 and the frontend's optimistic insert both depend on it and nothing had stated it. Still asserted, not derived: ~3,500 docs/s indexer capacity, ~25,000 reads/s post-store read budget, p50 1 s / p99 30 s fanout lag, ~1 s tombstone replication |
| Wednesday (§9 drafting) | Asked Claude to write §9, security | Seven subsections framed around three assets (authorship integrity, availability at ~3x headroom, the deletion promise) rather than confidentiality, since every post is public. Threat table for sessions reusing the §5.2 scheme; ownership checks placed in the post service with the 15-minute edit window treated as an authorisation boundary; rate-limit counters in Redis keyed per `sub` or per IPv6 /64, failing open; image handling covering presign scoping, magic-byte validation, SVG rejection, an 8,192 x 8,192 decode cap, re-encoding to strip EXIF and polyglots, and a sandboxed decoder; escaping at render not at write; a table of what deletion actually removes; TLS 1.3 and mTLS. Arithmetic checked in the shell: 20,000,000 x 300 posts/hour = 1,666,667/s against a 1,736/s design peak (960x), and 8,192 x 8,192 x 4 B = 256 MiB of RGBA | Kept. The two parts I want to be able to defend out loud are (a) per-user rate limits are 960x short of protecting the fleet, so the real controls are global admission shedding and account-age gating — asserting "we rate limit" without that arithmetic is the generic answer the prompt warns about; and (b) "purged from every timeline in 24 h" is a promise about the serving path, not every byte — the event log's 7-day retention and 30-day backups outlive it, and I chose to write that down rather than claim crypto-shredding I have not designed. Also kept rejecting SVG outright and rendering bodies as React text nodes, which is the constraint the frontend has to honour. Still asserted, not derived: the 10 failures/account/hour login throttle, ~20,000 rate-limit evaluations/s at peak, 7-day log and 30-day backup retentions (§10 must state the same numbers or the sections disagree), and the account-age tier of 10 posts/hour |
| Wednesday (§10 drafting) | Asked Claude to write §10, deployment and observability | Three deployment classes (stateless request-path, stateful consumers, stores) with different release mechanisms; the two SLOs stated as 99.9% of home-timeline reads under 400 ms over 28 days and p99 publish-to-searchable under 5 s; a metrics table where each signal is tied to the §7 bottleneck it leads; a page/ticket/not-an-alert split; and six named telemetry artefacts chosen specifically so the §12 dispute ("I saw the old text on page 3") is answerable from telemetry. Arithmetic in the shell: a 1% canary at peak is 174 req/s so a 30-min bake sees ~312,000 requests; a 30 s consumer rebalance during a deploy costs 2,604,180 entries of backlog draining in 8.3 s at §8.1's 313k/s surplus; the error budget is 15M requests or 43.2 minutes a month; 1% head sampling is 6.2M traces/day at 12.4 GB, and ~400 B x 670M requests/day is 268 GB/day or 8 TB at 30-day retention | Kept. The three things I want to defend out loud: (a) the bake duration and canary fraction are derived from traffic, not habit — 312,000 requests is what makes a sub-percent error regression detectable; (b) `degraded: true` counts as a **success** for the availability SLO, because an SLO that punishes graceful degradation teaches the system to fail hard, and §5.1 already made degradation a 200; (c) the revision-per-hydrated-post span attribute is the one piece of instrumentation without which the §12 complaint cannot be settled — the trace would otherwise prove nothing. Also closed the consistency debt §9 opened: 30-day telemetry retention is now stated here and matches §9.6, and the event log's 7-day retention is called out as a separate number that *does* contain bodies. Still asserted, not derived: the 400 ms target, the 30 s rebalance pause, 400 B per structured log line, 2 KB per trace, and the 2%/1 h and 5%/6 h burn-rate thresholds |
| Wednesday (§11 drafting) | Asked Claude to write §11, the five decision records, using the fixed template and citing numbers already established in §§2–10 rather than inventing new ones | Five records: hybrid fanout, edit propagation by reference, asynchronous search indexing, two-phase deletion, direct-to-object-store images. New arithmetic computed in the shell: the 100,000 threshold **derived** from a budget rule (no single post may cost more than a quarter of one cluster-second: 0.25 × 400,000 entries/s = 100,000 followers; 200,000 at 16 shards); a 3M-follower post = 7.5 s of the whole cluster against 104 s of the average fanout budget; edits at 3,500,000/day = 40.5/s, which under a denormalised design would be 2,025 rewrites/s average and 3,000,000 for one celebrity edit; timelines holding bodies would be 20M × 800 × 700 B = 11.2 TB against 384 GB at 24 B/entry (29x); image ingress through the API tier = 260 uploads/s × 2 MB = 521 MB/s = 4.2 Gbit/s; purge at 34.7 deletes/s = 0.009% of cluster capacity | Kept all five. The one I most wanted out of this pass is the threshold derivation — §3.3 and §7.2 had been *using* 100,000 as an asserted constant since the §3/§4 pass, and the journal already flagged that as debt. It is now derived from the cluster's op budget and the 1,736 posts/s peak, which also explains why the dial moves to 200,000 on a reshard. Kept the deliberate symmetry that every "what would change my mind" names a metric that §10 actually collects (fanout lag p99 > 30 s, cache hit rate < 92%, p99 publish-to-searchable > 4 s, CDN hit rate < 95%). Changed nothing in §§3–10: I checked each cited figure back to its source section rather than letting the records restate numbers loosely. Still asserted, not derived, and now written into §11.1 as such: wide accounts holding ~5% of an average viewer's 50 follow edges, which is what §3.4's "0–3 wide followees" silently assumes — flagged for §13 |
| Wednesday (§12 drafting) | Asked Claude to write §12, the worked trace, against the exact prompt scenario (3M-follower account edits 10 minutes after publishing while a follower paginates), reusing §5 payloads and §4 component names | Seven numbered steps with real request/response bodies, a component-state table after each, the direct answer, an eight-row inconsistency-window table and two near-misses. Identifiers derived in the shell rather than invented: decomposed the §5.0 example post ID `1827639201234567890` under the §3.3 Snowflake layout (ms field 435,743,141,468 / shard 676 / seq 722), recovered the implied service epoch 2012-11-26T02:14:18.532Z, then minted every other post ID from its timestamp against that epoch and base64-encoded the three cursors so they actually decode to `{"v":1,"b":"<id>"}`. Also: 1 ms = 2^22 = 4,194,304 ID units; the page-3 cursor gap 1827639201234567890 − 1827627260048323001 = 11,941,186,244,889 units = 2,847 s, which is *why* P cannot reappear; cache-invalidation load on a famous post = 3,000,000 followers × 20% active × 25 loads/day ÷ 86,400 × 3 peak = 521 req/s × 5 ms = 2.6 concurrent misses | Kept. The answer is "once, one version, revision 1", and it falls out of two facts already in the design rather than being asserted here: grace is wide so P is never in any timeline list at all (0 fanout writes on publish, 0 on edit), and edits do not change `post_id` so P cannot move between pages. The part I most want to be able to say out loud is the qualification — rob's *screen* is mixed-vintage for 150 s, and if he opens the permalink or search in that window he sees two revisions of the same post at once. That is the §8.7 "no monotonic reads" give-up made concrete, and hiding it would have been the easy mark to lose. Kept the contrast in each table row against the §11.2 alternative (7.5 s of cluster-wide rewrites racing the original fanout) because the trace is the best place to show what the decision bought. Changed nothing in §§3–11: every figure and field name in the trace was checked back to its source section (`degraded`, `edit_count > 0` as the edited indicator, `editable_until` only for the author, `ETag`/`If-Match`, `retryable: false` on `edit_window_closed`). Still asserted, not derived: the 20% active-follower fraction and the 5 ms post-store read latency in window 7, and the ~820/900 ms indexer lag, which is the §11.3 budget rather than a measurement |
| Wednesday (§13 drafting) | Two-step. First asked Claude to draft my own three weakest parts from the document and the "still asserted, not derived" trail in this journal. Separately dispatched a `general-purpose` agent to read §§2–7, §11 and §12 **cold**, with no framing from me, and return ranked failures with arithmetic and a named failure threshold each | My own three: the 0–3 wide-followees assumption, the 95% cache hit rate, and the timeline store having no durable copy. The cold agent returned 9 findings. It reached the wide-followee problem independently by a different route (80% of active edges point at top accounts → 40 per viewer, against my 20B/500M → 40) and added the part I had missed entirely: the pull path's range reads are never charged against the post store's 25,000/s budget, so at 2–3 wide followees the read path is already 1.4–2.1x over at the design point. Its sharpest finding was one I had no idea about — §7.2's three "plausible mixes" imply mean fanouts of 110–170 against the constraint's 50, because aggregate fanout is identically posts/s × mean; a distribution changes burstiness, not the total. So "the mean-based 4.6x is a fiction" is itself the fiction. It also found that 400,000 entries/s consumes 100% of the 800k ops/s budget with nothing left for reads | I verified the four highest-stakes claims against the file myself before accepting any of them: §2 really does contain only §2.1 (315 words, none of the required capacity arithmetic); §3.3:195 really does say "the same transaction" across four partitions; §11.4:2105 really does price a 3M-follower purge at 3,000,000 timeline entries when §11.1 makes that account wide and §12 says it has zero; §3.2/§3.4 really do filter "deleted-or-unfollowed" against a `tomb:authors` that §6.9 defines as deleted-only and sizes at 2,000 entries. All four hold. **Discarded** two things the agent asserted: it replaced my unsourced 25,000 reads/s with its own unsourced 10k point-reads/s/node — the critique is right, the replacement figure is not evidence, so §13.2 now says the number could be wrong in either direction rather than naming a substitute; and its purge arithmetic converts LREM's 800-element scan into 800 "ops" against a 100k ops/s/shard budget, which is not the same currency and would not survive the debrief. **Kept** for §13: weakness 1 is mine, sharpened with the agent's pull-path budget arithmetic; weakness 2 merges my cache-hit-rate critique with its observation that the capacity side is equally unsourced; weakness 3 is entirely the agent's — the 25 sessions/day parameter, which I verified myself (4x re-read factor, breakeven at 36.0 sessions/day, and 8,680 users at the §5.5 limit of 120/min reproducing the whole designed peak). **The rest of its findings are repairs, not self-critique, and are not in §13** — confessing something I could have fixed is the wrong use of that section. The root cause of most of them is one thing: §2.1 adopts a reading of the fanout/skew tension and §§7, 11 and 12 all revert to raw follower counts |
| Wednesday (§2 repair) | The cold-read agent's finding 6 was that §2 contained none of its required content — 315 words, only §2.1, with the actual capacity arithmetic living in §7.0, which described itself as "restated here" from a §2 that never stated it. §1.3 also pointed at §2 for two figures that were not there, and §4.12/§11.5 cited "(§2)" for the 405 TB image figure. I verified this myself by word-counting the section before acting on it. Asked Claude to write the missing §2.2–§2.7 | Write rate (579/s, 1,736/s at 3x, 2.5 posts/active user/day); fanout (28,935 entries/s, 86,806 at peak, 2.5B/day); the skew case (a 1M-follower post = 2.5 cluster-seconds, 20,000x the average post); a five-row storage table treating post bodies, edit history, images, search index and timelines separately; the ratio at 12.4:1 with the non-circular cross-check; and image egress at 300 TB/day, 10.4 GB/s peak, with the 95%-vs-98% CDN hit arithmetic | Kept. Every figure was computed in the shell and checked against the section that already cited it, so §2 restates nothing loosely: 540 GB/1.62 TB matches §4.3 and §6.2, 38 GB of revisions matches §6.3, 405 TB matches §4.12, 0.74 TB of search index matches §6.7, 384 GB of timelines matches §4.7, and every peak figure matches §7.0 to the digit. Three things I want to be able to defend out loud. (a) §2.3 now states that aggregate fanout is `posts/s × mean` **identically**, so a distribution changes burstiness but not the total — which contradicts §7.2's mixes, and I said so in §2 rather than leaving the two sections to disagree silently. (b) §2.4 gives the skew case in nominal follower counts to stay consistent with §11.1, then names the 25x gap against §2.1's own adopted reading and hands it to §13.1 — disclosing the inconsistency is worth more than quietly picking a side. (c) §2.6's cross-check **passes only because of a 4x re-read factor** I had to name to make it pass; that is the honest result and it is the thing §13.3 attacks. Changed nothing in §§3–12: where §2 disagrees with a later section, it says so and points at §13 |
| Wednesday (§2 audit) | Challenged the §2 I had just written against the prompt's own instruction — "These numbers are the assessment. Use them exactly. Do not substitute figures from any other system you have read about" — and asked for a line-by-line audit against `PROMPT.md` rather than against anyone's memory of it | All nine constraint-table values used exactly, and everything purely derived from them checks out. Three problems. (1) A real violation: §2.6 justified my 12.4:1 ratio with "public social feeds are often quoted at 100:1 or higher" — an unsourced figure from another system, doing actual argumentative work. (2) "Top 0.1% of accounts" is ambiguous and my own §1.3 assumption makes it bite: §2.1 reads "accounts" as the 20M actives (20,000 wide accounts, a 20x gap), but the 500M registered accounts that §2.1 itself introduces would make it 500,000 wide accounts and a 500x gap. (3) The §2.5 storage table used a 30.44-day month for posts and a 30-day month for images | All three fixed. (1) Deleted. The replacement argues from scope instead — §1.2 puts logged-out reading out of scope, so every read counted is authenticated, and I explicitly decline to put a number on what anonymous traffic would add because there is nothing in the table to derive one from. Saying "I don't have a number for that" is worth more than borrowing someone else's. (2) This is the one I am glad the audit caught, because it is the prompt's own "two figures pull against each other" test and I had only spotted half of it. The reconciliation is **self-referential**: the 500M assumption introduced to explain 1M-follower counts also multiplies by 25 the number of accounts required to have them. §2.1 now states both readings, shows the 20x vs 500x arithmetic, and says I design against 20,000 — because it is the conservative choice for the decision the number drives (fewer wide accounts means more posts stay on the push path, so the fanout budget is not flattered) and it keeps one meaning of "accounts" across §§3–12. §13.1 updated to match: 40 wide followees is the *favourable* number, the alternative reading gives 1,000, and no reading gets near the 0–3 the design assumes. (3) Images moved to the 30.44-day month: 228.3M/month, **411 TB**, propagated to all six sites that cited 405 TB (§2.5 twice, §4.9, §4.12, §11.5 twice) so the figure does not drift between sections. Total durable growth 413 TB/month |
| Wednesday (assumptions audit) | Asked Claude to go back over the deliverable and fill this journal's empty "Assumptions I made" table with the genuine assumptions — the ones the prompt leaves to me, not anything substituting for a constraint-table figure | 28 numeric assumptions gathered from §§2–12 and ranked by how much weight each carries, with §1.3's qualitative assumptions left where they are and cross-referenced rather than duplicated | Kept. Two things I want to be able to say out loud about this table. First, the top five are ranked by load-bearing weight and three of them are the three §13 weaknesses — that ordering is the point, not decoration: 25 sessions/day, 0–3 wide followees and the 95%/25,000 pair are where the design is most exposed, and they are assumptions rather than derivations. Second, the 100:1 read-ratio figure is deliberately **not** in the table, because it was never a legitimate assumption — it was a borrowed figure from another system and the §2 audit deleted it. The table is for numbers the prompt hands to me, not numbers I took from elsewhere. One entry (near-threshold post mix, 0.1–0.5%) is listed with a note that I no longer believe the analysis it supports, since §7.2 still needs the repair §13 identified — listing it honestly beats quietly dropping it |
| Wednesday (§7.2 repair) | Fixed the defect the cold-read agent found: §7.2 claimed "the mean-based 4.6x is a fiction" and modelled three follower mixes to justify ranking fanout as bottleneck #1 at 1.4x headroom | Two errors, not one. (a) The three mixes implied mean fanouts of 110, 150 and 170 against the constraint's 50 — aggregate fanout is `posts/s × mean` **identically**, so a distribution changes how work arrives but not how much there is. I verified this by constructing two mixes consistent with mean 50 (0.10% at 10,000 followers, and 0.05% at 99,999) and both reproduce 86,800 entries/s exactly. (b) The 400,000 entries/s capacity figure spent 100% of the op budget on fanout with nothing reserved for the 17,361 LRANGE/s of reads or for operating headroom; at a 70% target with reads reserved it is ~254,000 entries/s | Rewrote §7.2 around the correction rather than papering over it — the retraction is in the text, because "an earlier draft argued the opposite and was wrong" is more defensible in a debrief than a silently corrected number. The real insight replaces the fake one: **the mean fixes the throughput, the tail fixes the queueing.** The constraint bounds lumpiness at 0.05% of posts from 99,999-follower authors = 0.87 posts/s at peak, each costing 0.39 cluster-seconds, giving the same 34% utilisation delivered as indivisible lumps. That is why 34% does not feel like 34% — service-time variance sets queueing delay, not mean utilisation. Consequence for the mitigations: head-of-line blocking is promoted from third to first, and I added chunking wide expansions into 20 × 5,000-entry work items, which is the thing that actually attacks variance. Carried the correction through every figure that depended on it rather than leaving drift: §7.1 row 1 (now 86,806 demand / 254k capacity / 2.9x), §8.1's backlog drain (156 s, not 83 s, because a cluster recovering from an outage is the last place to run flat out), §10's rebalance backlog (15.6 s, not 8.3 s), and §2.4 relabelled to say "ceiling" where it cites 400,000. Two honest consequences recorded in the text and **not** silently fixed: §7.1's ranking is no longer purely by headroom — the post cache at 1.4x is tighter than fanout at 2.9x, and I keep fanout at #1 for blast radius and silent failure, which means the table uses two criteria and says so; and §11.1's threshold derivation (`0.25 × 400,000 = 100,000`) gives ~63,500 against the sustainable figure, which I left for the §11.1 repair because re-cutting the threshold moves §11.1, §11.4 and §12 together |
| Wednesday (§14 drafting) | Asked Claude to help with the last section, trade-offs and what I would do with more time, kept short | A trade-off table (five deliberate trades: hybrid read path vs the 7.5 cluster-second post, ID-only timeline entries vs 347,222 hydrations/s, async index/purge vs the 5 s and 24 h windows, no read-your-writes, single region vs the 3x peak) and a five-item ordered next list whose first two items are measurements from §13 rather than code | Kept it. The ordering argument is the part I care about — items 1 and 2 are the two guessed numbers §13 already confesses to, and building the read-time merge fix (item 3) on top of them would be building against figures I have admitted are unsourced. Trimmed nothing; checked each trade cites a section that actually says it |
| Thursday (1b planning) | Starting Problem 1b. Gave Claude `frontend/SPEC.md` and §5 of the deliverable and asked for a plan document before any code, with the instruction to assume nothing and ask where the brief is genuinely open. Framework chosen by me: Vue | It classified the work as a new project rather than a bounded change, asked four questions instead of guessing (mock-layer shape, state approach, which contract endpoints to exercise beyond the four required behaviours, tests yes/no), then wrote `frontend/PLAN.md`: module layout with a contract-section mapping per module, the three §5.0 type details, store shape, the optimistic-vs-pagination race argument, error rendering rules, mock controls, four test cases, an 11-step build order and a cuts list | Kept the plan. My four answers: Vite dev middleware for the mock (real HTTP means real status codes, `ETag`, `Retry-After` and a genuine `?fault=` query param — an in-process fixture layer would have made the error path a code branch, which is weaker evidence that the client matches §5); Pinia over a composable, because the announced debrief question is the optimistic-write/"load more" race and a single named owner lets me answer it from one file; revisions view, edit-in-window and the `degraded` banner in, because §5 specifies frontend behaviour for each by name; silent token refresh out, because it overlaps the auth UI SPEC excludes. Two things I pushed back on and settled myself: I kept **two** error slots (`error` vs `loadMoreError`) despite the extra state, because collapsing them forces a choice between blanking a list the reader is looking at and hiding the failure entirely; and I hardcoded the viewer identity rather than adding a `/v1/me` endpoint, since inventing an endpoint that is not in §5 is exactly the contract drift this section is marked on. The part of the plan I most want to defend out loud is §4: the race resolves with no cancellation and no locking, and the argument is entirely §5.3's own — pagination moves toward lower IDs, a new post gets a higher ID than the captured cursor, so the two writes are disjoint. The dedup guard on reconcile is the non-obvious half: without it a timed-out-then-retried publish renders twice, because §5.4 returns 201 with the original body on an idempotent replay. Nothing written yet beyond the plan; code starts after review |
| Thursday (1b task breakdown) | Asked Claude to turn `frontend/PLAN.md` into an executable task plan before writing code — bite-sized steps, real code in every step, no placeholders, and every step verifiable. Gave it `SPEC.md` and §5 in full | `frontend/BUILD-PLAN.md`: ten tasks following PLAN.md §9's build order, each with exact file paths, a Consumes/Produces interface block, and actual code rather than descriptions — the full `tsconfig.json`, `api/types.ts`, `api/errors.ts`, `api/http.ts`, `api/client.ts` and the store, plus 16 Vitest cases written out in full, `curl` commands with the expected status codes and bodies, and a browser state-by-state checklist. It also re-derived §12.0's Snowflake and cursor arithmetic in the shell to confirm the mock's ID generator would reproduce `1827639201234567890` and `eyJ2IjoxLCJiIjoiMTgyNzYzOTIwMTIzNDU2Nzg5MCJ9` exactly | Kept it. Four things I changed or decided myself. (a) **Commit steps propose a message and stop** — the skill's template commits automatically, and the commits here are the audit trail, so they are mine to approve. (b) **Tests are store-level only, and the plan says that is a deliberate narrowing of the usual test-first rule** rather than leaving a reader to assume I forgot; `SPEC.md` makes tests optional and PLAN.md §8 already scoped them. (c) I had it reuse **§12's identifiers** — grace/88213004, rob/41777219, post `1827639201234567890` — for the fixture corpus, so §12 and the running app can be read side by side; that was not in PLAN.md and it is the cheapest consistency win available. (d) I added `?fault_after=<n>` to the mock controls, because without it the *load-more* failure is unreachable by query parameter and the two-error-slot design in PLAN.md §3 cannot actually be demonstrated. Two contract details the breakdown forced me to settle that PLAN.md had left vague: the `If-Match` value is derived from `post.revision` rather than stored from the `ETag` header, since §5.1#4 defines the ETag as exactly `"<revision>"` and one source of truth is better than two; and **edit is not optimistic while publish is**, because §5.0 makes `editable_until` advisory and re-checked server-side, so the response is the only honest answer — that asymmetry is deliberate and goes in the README |
| Thursday (1b plan verification) | Before writing any code, asked Claude to verify the two plan docs — `frontend/PLAN.md` and `frontend/BUILD-PLAN.md` — against §5 of the deliverable and against each other. Then asked it to fix five of the findings | Every §5 quotation in both docs checked out against the source lines, including the one the race argument rests on, and §12's identifiers matched the fixture corpus. Seven findings: (1) the two plan docs contradict each other on rollback; (2) `publishError` is one store-level slot for an array of pending entries; (3) `restart()` does not fence an in-flight `loadMore()`; (4) `parseApiError` has two different retryable fallbacks in the same function, one of them excluding 429; (5) `PAGE_LIMIT = 20` cites §5.3, but `limit` is defined in §5.1 endpoint 2; (6) PLAN.md §1's file layout and §8's test list had gone stale against BUILD-PLAN.md; (7) §5 says "every `Post` read carries `ETag`", which is meaningless for a 20-post page | Fixed 1, 2, 4, 5, 6 in the plan docs and left 3 and 7 for now. The two that matter. **(1)** PLAN.md said rollback unconditionally removes the pending entry, and in the next paragraph said a retry reuses the same `Idempotency-Key` — those cannot both be true, because removing the entry removes the key with it. BUILD-PLAN.md had already resolved it by only rolling back terminal failures; PLAN.md was the stale one, so I rewrote §4 around the split and wrote the reading into the README task: SPEC's "rolls back on failure" is implemented for terminal failures only, and a retryable one keeps the entry *because* §5.4 makes the key the thing that makes a retry safe. That is an interpretation, not an omission, and saying so beats being asked about it. **(2)** is the same mistake I had already argued my way out of once: PLAN.md §3 justifies two error slots for page-1 versus page-N because one slot forces a choice between blanking the list and hiding the failure — and then I put a single slot on a store whose `pending` is an array, so composing a second post silently wipes the error on a first entry still sitting there with a "Try again" button. Moved retryable errors onto `PendingPost.error` and kept `composeError` for terminal ones, which is the same argument applied twice instead of once, and added two regression tests that fail on the old shape. Finding 3 (a stale `loadMore` can concat onto a freshly restarted list, reachable via the "Start over" affordance) is real and I want to fix it with a generation counter, but it is a design addition rather than a correction, so it goes in when I write the store. Finding 7 is a change to §5 itself and I would rather batch it with the §11.1 threshold repair than touch the contract twice |
| Thursday (1b build, tasks 4–10) | Ran `BUILD-PLAN.md` tasks 4 through 10 in one session: HTTP layer and client, the timeline store, components, optimistic publish, the edited indicator and revisions panel, in-window edit, and the README. Test-first for the store, as the plan scoped. I pre-approved per-task commits so the audit trail stayed one commit per task rather than one squashed lump, and told it to drop § citations from commit messages | Seven commits, 21 Vitest cases passing, `vue-tsc --noEmit` clean, no `any` in `src/` or `mock/`, and a clean-clone `npm install && npm run dev` verified in a temp directory. It found two genuine contract-versus-code gaps on its own: the fixture clock and the corpus disagreed about which posts were still editable, and the fault table stopped at the four consumed endpoints while §5.5 promises every code in the table is reachable without editing code | Kept, with three decisions of my own. (a) **The fixture clock.** The pin was at 10:05, which left rob's 09:56 post editable even though the corpus omits `editable_until` on it — the server and the fixture data were telling the reader two different things. Repinned to 10:12, which sits between the two window boundaries in the corpus (10:11 and 10:14), so exactly one post is editable no matter when this is run. The commit says the old pin was wrong rather than quietly moving it. (b) **The fault table** now carries every §5.5 code including the ones for search, follow, deletion and media — endpoints this view never calls. A reviewer typing `?fault=search_unavailable` and getting `validation_failed` would be reading a contract violation, and the rows cost nothing. (c) **Browser verification is mine, not the assistant's.** It offered to build gstack's headless browser; I declined and took the state walkthrough myself, because the debrief asks me to walk through this code and its behaviour from memory, and "an agent ticked the checklist" is not an answer. The one thing I want to be able to defend out loud is still the race: `reconcile` puts a confirmed post at the head while `loadMore` only ever appends at the tail, and the dedup guard covers the idempotent-replay case — both resolution orders are tested, and neither needs cancellation or a lock |


---

## Assumptions I made

Every assumption you made that the prompt did not settle. One line each, with the value you chose.

The prompt fixes ten figures and says "every other number you need is yours to assume. State each
assumption where you use it." §1.3 of the deliverable carries the *qualitative* assumptions
(scope, semantics, ordering). This is the **numeric** set — every number in the deliverable that I
chose rather than derived from the constraint table. All of them are stated at the point of use in
the deliverable as well; this table is the single place they can be counted.

Ordered by how much weight each carries. The top five are the ones I would attack first, and three
of them are §13.

| Assumption | Value I chose | Why |
|------------|---------------|-----|
| Timeline sessions per active user per day | **25** | The prompt withholds the read-to-write ratio and requires one. This is the number the whole read side descends from: 500M reads/day, 12.4:1, 17,361 req/s peak, 347,222 hydrations/s. Cross-checked in §2.6 against posts actually arriving (125/day), which passes only via a 4x re-read factor. **§13.3** — fails above 36/day |
| Wide accounts in an average viewer's 50 followees | **0–3** | What makes the read-time merge affordable. It is the assumption §2.1's own reconciliation contradicts (40, or 1,000 under the alternative reading of "top 0.1% of accounts"). **§13.1** — I would attack this before anything else |
| Post cache hit rate | **95%** | Sizes bottleneck #2. The load is the *miss* rate, so this is a lever: 92% already puts the post store over budget, a cold shard is 14x. **§13.2** |
| Post store read capacity | **~25,000 reads/s** | Used as a fixed budget to rank bottlenecks against, which made §7.1 look quantitative when its denominator was a guess. Also the sole justification for a replica per timeline shard (§8.4). Sanity-checking against §4.3's storage suggests it may be too low. **§13.2** |
| Redis ops/s per timeline shard | **100,000** | With 8 shards, this produces the 400,000 entries/s that every fanout figure in §7, §11 and §12 divides by. A technology characteristic rather than a product figure, but unverified — the one I would benchmark before the debrief |
| Peak multiplier | **3x the 24-hour mean** | A single-region product concentrates traffic into roughly an 8-hour band. Applied to every mean in the document |
| Registered accounts | **~500M** | Needed for 1M-follower counts to be possible at all (§2.1). Introduces the ambiguity in "top 0.1% of accounts" that §2.1 now names explicitly |
| Profile and permalink views | **5 per user/day** | Second term of the 12.4:1 ratio |
| Search queries | **1 per user/day** | Third term of the 12.4:1 ratio. Deliberately low — search is not the product's main loop |
| Average post body | **140 characters** of the 500 allowed | Drives the 355 B row and therefore all post storage. Short-form posting clusters well below the cap |
| Post row on disk | **355 B** (222 B of fields × 1.6 wide-column overhead) | 1.6x is the Cassandra per-cell overhead for column names and cell timestamps |
| Posts that get edited | **5%**, at **1.4 edits** each | Gives 107M revision rows/month, 7% on top of post bodies. Both are guesses; the 15-minute window bounds the blast radius either way |
| Posts carrying an image | **15%** | Drives 411 TB/month of storage and 300 TB/day of egress — the largest numbers in the system |
| Average image size | **1.2 MB** against the 2 MB cap, **1.5x** for original + 2 variants | Uploads cluster below a cap rather than at it. The 1.5x is the fixed variant set of §11.5 |
| Delivered image variant | **200 KB**, ~3 per 20-post page | Turns the 15% image rate into the egress figure |
| Search document | **210 B** indexed, **2.3x** for inverted index and doc values | Gives 0.74 TB/month per replica |
| Materialised timeline depth | **800 entries**, **24 B** per entry | Fixes the timeline store at 384 GB and makes it a bounded working set rather than a monthly accrual. 800 entries is ~6.4 days at the modelled inflow |
| Posts per timeline page | **20** | Turns 500M requests/day into 10B hydrations/day. Also the page size in §5.3 and the frontend |
| Follow edge on disk | **26 B**; **10,000 edges** per partition bucket | Bucketing is forced by Cassandra's ~100k-row partition guidance, not by bytes — 3M followers is 3M rows |
| Search indexer capacity | **~3,500 docs/s** | Gives search 2x headroom on throughput, which is why §7.4 argues it breaks on lag rather than capacity |
| CDN hit rate | **95–98%** | Three points is 9 TB/day of origin egress — a bill, not an outage |
| Fanout lag | **p50 1 s, p99 30 s** | The 30 s figure is the §10 page threshold and the "what would change my mind" trigger in §11.1 |
| Daily account deletion rate | **0.01%** of actives | Gives a 2,000-entry tombstone set at 16 KB, replicated to every timeline instance |
| Active fraction of a wide account's followers | **20%** | Used in §12 to size cache-invalidation load on a famous post (521 req/s) |
| Posts from near-threshold authors | **0.1–0.5%** at 20k–60k followers | The §7.2 mix. Since writing §13 I no longer think this analysis is sound — aggregate fanout is `posts/s × mean` identically, so these mixes imply a mean of 110–170 against the constraint's 50. Listed here because it is still in §7.2 and still needs repair |
| Login failure throttle | **10 per account/hour** | §9.2. Chosen to be survivable for a fat-fingered user and useless for credential stuffing |
| Timeline latency target | **400 ms** at p99 | §10.2 SLO, and the threshold three §13 tests measure against |
| Telemetry | **1% head sampling**, **400 B** per log line, **2 KB** per trace, **30-day** retention | Gives 12.4 GB/day of traces and 8 TB of logs. The 7-day event log retention is separate and is the one that contains post bodies (§9.6) |

---

## What I would do with another day

Three to five lines. Be specific.
